table seq
col lastval integer

table nodes
col id integer
col mac varchar
col last_ts timestamp

table sessions
col session_id varchar
col session varchar
col updated timestamp

table users
col user_id integer
col email varchar
col passwd varchar
